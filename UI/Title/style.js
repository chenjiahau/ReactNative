import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  container: {
    height: 32,
    marginVertical: 16,
  },
  label: {
    color: '#000000',
    fontSize: 28,
    fontFamily: 'black',
  },
});

export default style;